# Only needed when wiring the live database (DATA_SOURCE=db).
# The prototype runs against static inventory by default — no DB required.
DATABASE_URL=postgres://user:password@localhost:5432/woodgreen

# "static" (default, no DB) | "db" (query Drizzle/Postgres)
DATA_SOURCE=static
