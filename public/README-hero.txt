HERO IMAGE — drop your watercolor here.

Save the watercolor of the building as:

    public/hero-building.jpg

(exact filename, all lowercase). It's used as the hero background behind a dark
scrim, so the cream headline stays readable (WCAG AA). A wide landscape image
(~1600px+ wide) works best — the building sits on the right, text on the left.

Until the file is added, the hero falls back to a solid deep-walnut background and
still looks fine. Once it's in place, commit + push and Vercel will serve it.
